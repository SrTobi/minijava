// This file is configured by CMake to substitute configuration values
// and project mata-information.  It should not be used directly but
// only via `global.hpp`.

/** @brief Project name. */
#define MINIJAVA_PROJECT_NAME "@PROJECT_NAME@"

/** @brief Project version string. */
#define MINIJAVA_PROJECT_VERSION "@PROJECT_VERSION@"
