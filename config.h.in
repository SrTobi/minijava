// This file is configured by CMake to substitute configuration values and
// project mata-information.  It should not be used directly but only via
// `global.hpp`.

/** @brief Project name. */
#define MINIJAVA_PROJECT_NAME "@PROJECT_NAME@"

/** @brief Project version string. */
#define MINIJAVA_PROJECT_VERSION "@PROJECT_VERSION@"

/** @brief `#define` to 1 if the `getrlimit` and `setrlimit` functions are available. */
#cmakedefine MINIJAVA_HAVE_RLIMIT 1

/** @brief `#define` to 1 to enable support for line-comments. */
#cmakedefine MINIJAVA_LINE_COMMENTS 1
